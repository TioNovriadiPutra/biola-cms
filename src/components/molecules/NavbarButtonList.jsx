import { StyleSheet, Text, View } from "react-native";
import React from "react";

const NavbarButtonList = ({ listItem }) => {
  return (
    <View>
      <Text>NavbarButtonList</Text>
    </View>
  );
};

export default NavbarButtonList;

const styles = StyleSheet.create({});
